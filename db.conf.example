# =============================================================================
# Zap Negócios — db.conf.example
# =============================================================================
# Arquivo de configuração do backend PHP.
#
# INSTRUÇÕES:
#   1. Copie este arquivo para db.conf (mesmo diretório)
#   2. Preencha os valores reais abaixo
#   3. NUNCA envie db.conf para repositórios públicos (git, etc.)
#   4. Configure o servidor para bloquear acesso direto a arquivos .conf
#
# Autor:   Ricardo Macari
# Contato: macari@gmail.com
# Projeto: Zap Negócios
# =============================================================================

# Credenciais do banco de dados MySQL
DB_HOST=localhost
DB_NAME=seu_banco
DB_USER=seu_usuario
DB_PASS=sua_senha

# Origens permitidas para requisições CORS (CRM e WhatsApp Web)
ALLOWED_ORIGIN=https://travelflow.tur.br,https://web.whatsapp.com

# Chave de API da instalação — primeira camada de proteção da API
# Gere com: openssl rand -hex 32
API_KEY=substitua_por_uma_chave_secreta_longa_e_aleatoria

# Chave de setup — usada apenas para criar o primeiro usuário owner
# no endpoint setup_owner.php. Gere com: openssl rand -hex 32
SETUP_KEY=substitua_por_uma_chave_de_setup_longa_e_aleatoria

# Chave legada para versões antigas da extensão. A versão atual usa usuários
# e permissões; mantenha vazia ou remova se todos já estiverem na versão nova.
ADMIN_KEY=legado_opcional_para_versoes_antigas

# Email usado como remetente dos alertas de tarefas atrasadas.
# No Plesk, prefira uma caixa existente do domínio.
MAIL_FROM=nao-responda@seudominio.com
